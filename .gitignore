# compiled output
node_modules
/dist
/build
/bindings
packages/*/dist
packages/*/dist-test
packages/*/build
packages/*/bindings

# packet capture samples for test-fixture extraction (local only)
/pcaps

# local AI assistant guidance (not committed)
CLAUDE.md

# local design drafts (not committed; per-package README + docs are the real deliverables)
/design

# Playwright MCP scratch output (browser verification runs)
/.playwright-mcp

# local env files
.env.local
.env.*.local

# Log files
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
